class Runner(object):
   def __init__(self, numNodes, scatterWidth, failureInterval):
      self.numNodes = numNodes
      self.scatterWidth = scatterWidth
      self.failureInterval = failureInterval

   def setup(self):
      # TODO
      pass

   def failureProbOfDataLoss(self):
      # TODO
      return 0.0

   def recover(self, recoveryPeriod):
      # TODO
      pass
